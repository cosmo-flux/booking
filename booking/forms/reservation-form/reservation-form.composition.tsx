import { MarsReservationForm } from './reservation-form.js';
import { Container } from '@cosmo-flux/design.layout.container';
export const BasicReservationForm = () => {
  return (
    <Container maxWidth="sm">
      <MarsReservationForm />
    </Container>
  );
};
