import { MarsReservationForm } from '@cosmo-flux/booking.forms.reservation-form';
import { Container } from '@cosmo-flux/design.layout.container';
export const RedPlanetReservations = () => {
  return (
    <Container maxWidth="sm">
      <MarsReservationForm />
    </Container>
  );
};
